# Use OpenJDK 17 as the base image
FROM openjdk:17

RUN mkdir -p /employee-management-system
# Set the working directory inside the container
WORKDIR /employee-management-system

## Copy the JAR file generated by the Spring Boot project to the container
#ADD /docker/employee-management.jar ems.jar
ADD employee-management.jar ems.jar

## Expose the port that the Spring Boot application will listen on
EXPOSE 9090

## Define the command to run the Spring Boot application when the container starts
ENTRYPOINT ["java", "-jar", "ems.jar"]
